@if (!empty($errors) && count($errors))
    <div class="container">
        <div class="alert alert-danger text-center">
            There was a problem, please review the error messages below and try again.
        </div>
    </div>
@endif